@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using Morning.Value.Web.Site.Auth.Models
@model SignInViewModel
@{
    Layout = "_AuthLayout";
    ViewData["Title"] = "Sign In";
}
<div class="container min-vh-100 d-flex align-items-center justify-content-center">
    <div class="col-12 col-sm-10 col-md-7 col-lg-5">
        <div class="card shadow-lg border-0 rounded-3">
            <div class="card-body p-4 p-md-5">
                <a href="/" class="logo d-block py-2">
                    <span role="img" aria-label="Logo of Morning Value" title="Morning Value">
                        <img src="https://www.morningvalue.com/web/image/website/1/logo/Morning%20Value?unique=fc61ced"
                             class="img img-fluid d-block mx-auto" width="95" height="40" alt="Morning Value" loading="lazy">
                    </span>
                </a>

                <h1 class="h3 mb-1 text-center">Bienvenido</h1>
                <p class="text-secondary text-center mb-4">Inicia sesión para continuar</p>

                <form asp-controller="Auth" asp-action="SignIn"
                      asp-route-returnUrl="@(ViewData["ReturnUrl"])"
                      method="post" novalidate>
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>

                    <div class="mb-3">
                        <label asp-for="Email" class="form-label"></label>
                        <input asp-for="Email" class="form-control" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label"></label>
                        <input asp-for="Password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 py-2">Ingresar</button>
                </form>

                <div class="text-center mt-4">
                    <span class="text-secondary">¿No tienes cuenta?</span>
                    <a href="@Url.Action("SignUp", "Auth")">Crear cuenta</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}